TODO:
1. find out why for some visits, window.onload does not fire.
2. query interface: canvas drawing tool. Background = transparent.
   Before generating structure feature, each closed transparent area is filled with different color,
   and is fed into segmentation().

   Before generating colormap feature, we use the 3x3 grid
   generated by segmentation(), and determine the dominant color.

   If the dominant color is transparent, then the area is ignored and is not counted in the following query.